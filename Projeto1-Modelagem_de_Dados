Projeto: Modelagem de Dados — Análise de Vendas (Power BI)
Resumo

Este repositório contém a documentação da atividade de modelagem de dados para um projeto de análise de vendas de uma rede de lojas. Os dados vêm de arquivos CSV separados e precisam ser relacionados corretamente no Power BI para garantir análises consistentes e confiáveis.

Arquivos fornecidos

Vendas.csv — registros de vendas diárias.

Colunas: VendaID, DataVenda, ProdutoID, Quantidade, ValorTotal, LojaID, VendedorID (assumido quando necessário)

Metas.csv — metas trimestrais por vendedor.

Colunas: VendedorID, Trimestre, Venda (meta)

Produto.csv — catálogo de produtos.

Colunas: ProdutoID, NomeProduto

Observação: caso algum campo mencionado não exista no CSV original (por ex. VendedorID em Vendas.csv), certifique-se de adicionar/alinhar os arquivos ou documentar o ajuste.

Objetivo

Demonstrar a importância da modelagem de dados para análises corretas no Power BI.

Carregar os dados no Power BI e configurar relacionamentos adequados.

Identificar tabelas de fato e dimensão, definir cardinalidades e comportamentos dos relacionamentos.

Fornecer boas práticas, exemplos de medidas DAX e sugestões de relatórios.

Passo a passo (para incluir no documento ou apresentação)
1. Importar os dados

Abra o Power BI Desktop → Obter dados → Texto/CSV → importe Vendas.csv, Metas.csv, Produto.csv.

Verifique tipos de dados: datas como Data, valores numéricos como Decimal, chaves como Texto/Inteiro conforme apropriado.

2. Limpeza e transformação (Power Query)

Normalizar nomes de colunas (sem espaços, use CamelCase ou snake_case).

Tratar valores nulos e duplicados (por ex. VendaID duplicado).

Converter DataVenda para tipo Data.

Se necessário, criar colunas auxiliares: Ano, Mês, Trimestre para facilitar junções com Metas.

3. Modelagem (Painel de Modelo)

Estabeleça relacionamentos baseados nas chaves:

Vendas[ProdutoID] → Produto[ProdutoID]

Vendas[VendedorID] → Metas[VendedorID]

Cardinalidade correta

Vendas → Produtos: Muitos para Um

Justificativa: Cada linha em Vendas representa uma transação (muitas) referenciando um único produto (um). Produtos aparecem muitas vezes nas vendas.

Vendas → Metas: Muitos para Um

Justificativa: Cada venda é atribuída a 1 vendedor; as metas são por vendedor (um registro ou por trimestre por vendedor), logo muitas vendas se relacionam a uma meta.

Impacto de cardinalidades erradas

Se inverter para Um→Muitos ou definir Muitos↔Muitos indevidamente, agregações ficarão erradas (duplicação de soma, filtros que não propagam corretamente, resultados inflacionados ou ausência de filtragem).

4. Direção do filtro e tabelas de início/término

Direção do filtro padrão: de dimensão → tabela fato.

Definir Produto como tabela de início (one/"um") e Vendas como fim (many/"muitos") para o relacionamento ProdutoID.

Para Metas, definir Metas como inicio e Vendas como fim.

Relacionamentos ativos: mantenha os relacionamentos principais ativos (Vendas↔Produto e Vendas↔Metas) para permitir propagação de filtros automática.

Relacionamentos inativos: use relacionamentos inativos apenas quando existirem múltiplas ligações plausíveis entre as mesmas tabelas (ex.: duas colunas de data — DataVenda e DataEntrega). Ative via DAX (USERELATIONSHIP) quando necessário.

5. Resolver ambiguidade e problemas

Ambiguidade ocorre quando o grafo tem múltiplos caminhos entre duas tabelas (caminhos circulares). Soluções:

Introduzir tabela de dimensão (ex.: DimData) e conectar todas as datas a ela (evita múltiplos caminhos diretos entre fatos e dimensões).

Remover ou desativar relações duplicadas e usar USERELATIONSHIP para ativação seletiva em medidas.

Criar tabelas ponte (bridge) quando houver relacionamentos muitos-para-muitos inevitáveis (por ex. produtos compostos, tags múltiplas).

Exemplo prático: se existir Loja com relação a Vendas e Metas e isso criar um ciclo, desative a relação secundária e peça filtragem via medida.

Identificação: Fato x Dimensão

Tabela de Fato

Vendas — contém transações/medidas numéricas (Quantidade, ValorTotal). É a fonte das métricas.

Tabelas de Dimensão

Produto — atributos dos produtos (NomeProduto).

Metas — embora contenha valores numéricos (meta de venda), na prática é uma dimensão temporal/organizacional (meta por Vendedor/Trimestre). Pode também ser modelada como dimensão ou fato agregado dependendo do escopo; neste exercício tratamos como dimensão (lookup) usada para comparar resultados.

Justificativa: fatos armazenam eventos transacionais; dimensões descrevem contexto/atributos usados para fatiar/filtrar as métricas.

🌈 Conclusão

A modelagem correta garante análises consistentes, visuais precisos e tomada de decisão eficiente no Power BI. 🎯✨

Este repositório é para fins didáticos. Sinta-se livre para adaptar o material.

Autor: (Seu Nome) — README gerado para atividade de modelagem Power BI
